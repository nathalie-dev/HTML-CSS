var nombre="";
var maximum="";
var nombreSaisis="0";

// demander 20 nombres et comparer au maximum, si le nombre est plus grand, il devient le nouveau maximum 
while (nombreSaisis<= 20);

//saisir un nombre
{
    console.log(nombre);
}

// si le nombre saisi est le premier saisi, maximum prend sa valeur
else (nombreSaisis=0);
{
    console.log( var maximum = var nombre);
}
